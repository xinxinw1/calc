Complex Number Calculator 2.0 - Aug 15, 2013 21:51

- split complex-math to libjs and renamed the app to "calculator"
- errors are now caught in the onsubmit function and displayed
- logs are handled by a callback in complex-math
- on a timeout, debug log no longer gets cleared
- all x = function (){} blocks now end with a ;
- display(origExpr, expr) -> display(orig, expr)
- debug link href and onclick moved to calculator.js
- checkSideWidth() now called in display function
