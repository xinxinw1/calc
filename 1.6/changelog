Version 1.6 - Nov 12, 2012 21:32

- fixed order of operations for -234^-345
- result is now rounded to prec before it is displayed
- replaced openNum with getA and getB
- replaced closeNum(exprarr) with N(a, b)
- shortened all complex function definitions
- pow now sends all real z and w with integer w to powr
- extended round, trunc, ceil, and floor to complex numbers
- fixed in roundr: nprec = undefined or 0 now works
- fixed bug in math ‘a,b’ to a+bi: ‘10,5’ now becomes 10+5i instead of 15i
